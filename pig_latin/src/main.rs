
fn pig_latin_word(word: &str) -> String {
    let mut new_word = String::new();
    let mut first_letter: char;
    let vowels = ['a', 'e', 'i', 'o', 'u'];
    if vowels.contains(word.chars().next())

}

fn main() {
    //Convert strings to pig latin

}
